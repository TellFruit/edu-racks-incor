import React from "react";
import {
  Route,
  BrowserRouter as Router,
  Routes as Switch,
  Navigate,
} from "react-router-dom";

const Routes = () => {

  return (
      <Router>
        <Switch>
        </Switch>
      </Router>
  );
};

export default Routes;
